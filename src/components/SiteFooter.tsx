export function SiteFooter() {
  return (
    <footer className="mt-24 hairline-t">
      <div className="mx-auto flex max-w-6xl flex-col items-center gap-1 px-5 py-8 text-center text-xs text-muted-foreground">
        <p>Historical results, not predictions.</p>
        <p>
          <span className="uppercase tracking-wider">Not</span> immigration advice.
        </p>
      </div>
    </footer>
  );
}
